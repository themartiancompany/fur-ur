---

# SPDX-License-Identifier: AGPL-3.0

#
#    .gitlab-ci.yaml
#
#    ----------------------------------------------------------------------
#    Copyright © 2022, 2023, 2024, 2025
#                Pellegrino Prevete
#
#    All rights reserved
#    ----------------------------------------------------------------------
#
#    This program is free software: you can redistribute it and/or modify
#    it under the terms of the GNU Affero General Public License as published by
#    the Free Software Foundation, either version 3 of the License, or
#    (at your option) any later version.
#
#    This program is distributed in the hope that it will be useful,
#    but WITHOUT ANY WARRANTY; without even the implied warranty of
#    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
#    GNU Affero General Public License for more details.
#
#    You should have received a copy of the GNU Affero General Public License
#    along with this program.  If not, see <https://www.gnu.org/licenses/>.


default:
  image: archlinux:latest

stages:
  - build

variables:
  PACKAGE_RELEASE: "${CI_COMMIT_TAG}"
  PACKAGE_REGISTRY_URL: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/${PACKAGE_RELEASE}"

.build:
  stage: build
  artifacts:
    paths:
      - dogeos-gnu-*.pkg.tar.*
    reports:
      metrics: output/metrics.txt
  before_script:
    - >
      pacman \
        -Sy \
        --needed \
        --noconfirm \
          archlinux-keyring
    
    - >  
      pacman \
        -Su \
        --needed \
        --noconfirm \
          bash \
          git \
          glab \
          jq \
          python-docutils \
          release-cli

    # Extra packages dependencies
    # archiso-encryption-git
    - >
      pacman \
        -Su \
        --needed \
        --noconfirm \
          asciidoctor \
          fakeroot \
          gnupg \
          make \
          shellcheck \
          shfmt

    # Save gl-dl private token in the
    # container
    - >
      mkdir \
        -p \
        "/root/.config/gitlab.com"
      echo \
        "${PACKAGE_REGISTRY_URL}"
      echo \
        "${GL_DL_PRIVATE_TOKEN}" > \
        "/root/.config/gitlab.com/default.txt"
      echo \
        "${BUR_GPG_KEY}" | \
        base64 \
          -d > \
        "/root/bur.asc"
      gpg \
        --import \
          "/root/bur.asc" || \
        true

    # Setup user
    - >
      useradd \
        "user"
      mkdir \
        -p \
        "/home/user"
      chmod \
        700 \
        "/home/user"
      cp \
        -r \
        "/builds/themartiancompany/${CI_PROJECT_NAME}" \
        "/home/user/${CI_PROJECT_NAME%-ur}"
      chown \
        -R \
        "user:user" \
        "/home/user"
  script:
    - ./.gitlab/ci/build.sh ${BUILD_SCRIPT_ARGS}

build:
  extends: .build
  parallel:
    matrix:
      - BUILD_SCRIPT_ARGS: arch x86_64 ${CI_PROJECT_NAMESPACE} ${CI_PROJECT_NAME} ${CI_COMMIT_SHA}
  only:
    refs:
      - master
      - merge_requests
    changes:
      - PKGBUILD
      - README.md
      - COPYING
      - .gitlab-ci.yml
      - .gitlab/ci/*
  interruptible: true

.upload:
  extends: .build
  after_script:
    - >
      for _file in "/root/dogeos-"*".pkg.tar"*; do
        _basename="$( \
          basename \
            "${_file}")"
        curl \
          --silent \
          --header \
          "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file \
            ${_file} \
          "${PACKAGE_REGISTRY_URL}/${_basename}"
      done

.release:
  extends: .upload
  after_script:
    - >
      printf \
        "%s\n" \
        "/root/dogeos-"*".pkg.tar"* > \
        "/root/packages.txt"
    - >
      for _file in $(cat "/root/packages.txt"); do
        printf \
          "%s " \
          "--assets-link" \
            "{ \"name\": \"${_basename}\", \"url\": \"${PACKAGE_REGISTRY_URL}/${_basename}\" }" >> \
          "/root/assets-links.txt"
      done
    - >
      for _file in $(cat "/root/packages.txt"); do
        _basename="$( \
          basename \
            "${_file}")"
        curl \
          --silent \
          --header \
            "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file \
            ${_file} \
          "${PACKAGE_REGISTRY_URL}/${_basename}"
      done
    - >
      release-cli \
        create \
        --name \
          "Release: ${CI_COMMIT_TAG}" \
        --tag-name \
          "${CI_COMMIT_TAG}" \
       "$(cat \
            "/root/assets-links.txt")"

release:
  stage: build
  extends: .release
  parallel:
    matrix:
      - BUILD_SCRIPT_ARGS: arch x86_64 ${CI_PROJECT_NAMESPACE} ${CI_PROJECT_NAME} ${CI_COMMIT_SHA}
  rules:
    - if:
        $CI_COMMIT_TAG
  # release:
  #   tag_name: $CI_COMMIT_TAG
  #   description: $CI_COMMIT_TAG
  interruptible: true
